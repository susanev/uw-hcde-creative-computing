function setup() {
  createCanvas(500, 500);
  textSize(40);
  // x,y is lower left-hand edge of baseline of text
  textFont("Barrio");
  
}

function draw() {
  background(255);
  text("hello", 100, 100);
}